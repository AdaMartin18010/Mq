# Prometheus RabbitMQ监控配置示例

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # RabbitMQ Exporter
  - job_name: 'rabbitmq'
    static_configs:
      - targets: ['localhost:15692']
    metrics_path: '/metrics'
    scrape_interval: 10s
