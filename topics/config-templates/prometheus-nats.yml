# Prometheus NATS监控配置示例

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # NATS监控端点
  - job_name: 'nats'
    static_configs:
      - targets: ['localhost:8222']
    metrics_path: '/varz'
    scrape_interval: 10s
